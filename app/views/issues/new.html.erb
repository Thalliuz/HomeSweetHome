
<br/><br/><br/><br/>
<div class="jumbotron jumbotron-fluid bg-primary text-white text-center">
    <h1 class="display-4">Create An Issue</h1>
    <h4 > To report an issue to your Landlord please fill out the form below.
    <br>
    <span class="text-danger">Note***</span> A text message will be sent to your Landlord once the issue is created.
    </h4>
</div>
<div class="container text-center">
  <div class="card">
    <div class="card-header">
      Name: <%=@user.full_name%>
      <br/>
      Address: <%=@user.address%>
    </div>
    <div class="card-body">
      <%= form_for(@issue) do |f| %>
        <%= f.hidden_field :name, value: @user.full_name%>
        <%= f.hidden_field :address, value: @user.address%>
        <%= f.hidden_field :user_id, value: @user.id%>
        <%= f.hidden_field :owner_id, value: @user.owner_id%>
        <div class="card-subtitle mb-2 text-muted"> - Description - <%= f.text_area :detail %></div>
        <%= f.submit :create, class: "btn btn-primary" %>

      <% end %>
    </div>
</div>
</div>
